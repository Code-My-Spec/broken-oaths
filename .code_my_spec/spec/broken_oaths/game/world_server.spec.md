# BrokenOaths.Game.WorldServer

GenServer, one process per world (Registry-addressed, lazily started) — owns live state, runs the send_after turn tick, applies commands, broadcasts diffs

## Type

module
