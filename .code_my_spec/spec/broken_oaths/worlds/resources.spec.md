# BrokenOaths.Worlds.Resources

Deterministic bonus-resource placement from the world seed and per-world density setting on eligible terrain (Cattle on grassland, Sheep/Stone on hills, Wheat on plains). Pure, seed-derived, no RNG — same seed+density yields the same resource map.

## Type

module
